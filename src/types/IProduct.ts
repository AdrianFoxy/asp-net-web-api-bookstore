export interface IProduct {
    id: number
    title: string
    pages: number
    format: string
    longDescription: string
    shortDescription: string
    amount: number
    price: number
    image: string
    isFavor: boolean
    resealeDate: Date
}